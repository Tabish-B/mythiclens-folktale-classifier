# MythicLens: Interpretable Folktale Classifier

MythicLens is a human-centered NLP prototype that uses interpretable text classification to explore stylistic and symbolic patterns in folktales and fairy tales.

The project focuses on transparent machine learning: instead of only predicting a class label, the model also surfaces the words and features that influenced its prediction.
